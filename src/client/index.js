import React from 'react';
import ReactDOM from 'react-dom';

import './style';


document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<span>Welcome to FlyRTC</span>, document.getElementById('content'));
});
